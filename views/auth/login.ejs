<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= name %> - Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="icon" type="image/png" href="/assets/favicon.png">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap"
      rel="stylesheet"
    />
    <style>
      /* Fade-in effect */
      .fade-in {
        opacity: 0;
        transform: translateY(20px);
      }

      /* Customizing input focus states */
      input:focus {
        border-color: #4c6ef5;
        box-shadow: 0 0 5px rgba(76, 110, 245, 0.7);
      }

      /* Customizing the button hover state */
      button:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      }

      /* Gradient Background */
      .bg-gradient {
        background: linear-gradient(to right, #4c6ef5, #3b82f6);
      }

      /* Set the GIF as the background */
      body {
        background-image: url('https://media2.giphy.com/media/v1.Y2lkPTc5MGI3NjExcW1peGU5bG1nYWNpbXc2eTEwc2FvZnBpeW83eHEyZWxpanFwNXViayZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/2PWBLDJ2KtB1X6o9vY/giphy.gif');
        background-size: cover; /* Make the background cover the entire screen */
        background-position: center; /* Center the background */
        background-repeat: no-repeat; /* Ensure the GIF does not repeat */
      }
    </style>
  </head>

  <body class="text-gray-300 min-h-screen flex flex-col justify-center">
    <main class="flex-grow flex items-center justify-center p-8">
      <div class="bg-white/10 p-8 md:p-12 rounded-3xl shadow-2xl max-w-lg w-full fade-in border border-white/20 backdrop-blur-lg">
        <div class="flex justify-center mb-6 fade-in">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            class="w-16 h-16 text-indigo-400"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M16.5 10.5V6.75a4.5 4.5 0 1 0-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H6.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z"
            />
          </svg>
        </div>

        <h2 class="text-center text-4xl font-extrabold text-white tracking-tight fade-in">
          Welcome Back!
        </h2>
        <p class="text-center text-gray-400 mt-2 text-base fade-in">
          Sign in to continue to your account.
        </p>

        <!-- User/Password Login Form -->
        <form method="POST" action="/auth/login" class="mt-8 space-y-6 fade-in">
          <div>
            <input
              type="text"
              id="username"
              name="username"
              aria-label="Username"
              class="flex-grow p-4 w-full bg-white/10 border border-white/30 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 text-white rounded-lg placeholder-gray-400 transition duration-200 ease-in-out"
              placeholder="Username"
              required
            />
          </div>
          <div>
            <input
              type="password"
              id="password"
              name="password"
              aria-label="Password"
              autocomplete="current-password"
              class="flex-grow p-4 w-full bg-white/10 border border-white/30 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 text-white rounded-lg placeholder-gray-400 transition duration-200 ease-in-out"
              placeholder="Password"
              required
            />
          </div>
          <button
            type="submit"
            class="w-full flex items-center justify-center gap-2 bg-gradient text-white font-semibold py-3 px-5 rounded-lg transition duration-300 transform hover:scale-105 shadow-md"
          >
            <span>Sign in</span>
          </button>
        </form>

        <!-- Register & Forgot Password Links -->
        <div class="text-center mt-4 text-sm fade-in">
          <p class="text-gray-400">
            Don't have an account?
            <a
              href="/register"
              class="text-indigo-400 hover:text-indigo-300 transition"
              >Register here</a
            >
          </p>
          <p class="mt-2">
            <a
              href="/auth/reset-password"
              class="text-gray-400 hover:text-indigo-300 transition"
              >Forgot your password?</a
            >
          </p>
        </div>
      </div>
    </main>

    <script>
      // GSAP animations for fade-in effect
      gsap.to(".fade-in", {
        opacity: 1,
        y: 0,
        duration: 1.2,
        stagger: 0.2,
        ease: "power2.out",
      });
    </script>
  </body>
</html>

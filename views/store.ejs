<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Store - Executorx Hosting Panel</title>
  <link rel="icon" type="image/png" href="https://yt3.googleusercontent.com/IVqzx0L3Ffpapttg-WK5yU9frk4AIVKixHnmmQjQ7RLrt4e66-7Ox6srKG_QVat5ceEq-98-s9s=s160-c-k-c0x00ffffff-no-rj">
  <script src="https://cdn.tailwindcss.com"></script>
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body class="bg-gray-900 text-white">

  <%- include('./components/template') %>

  <!-- Main Content -->
  <main class="flex-grow container mx-auto px-4 py-8" x-data="store()">
    <div class="flex justify-between items-center w-full mx-auto pb-4">
        <div class="flex flex-col">
            <h1 class="text-xl font-semibold mb-0.5">Store</h1>
            <p class="text-gray-400 text-md">Use your Coins to upgrade your servers by purchasing resources!</p>
        </div>

        <!-- Breadcrumbs -->
        <nav class="inline-flex py-2 px-4 rounded-xl bg-white/5">
            <ol class="flex items-center space-x-2">
                <li><a href="/" class="text-sm flex items-center gap-2 text-zinc-400 hover:text-zinc-200">🏠 Home</a></li>
                <li class="text-zinc-400">➜</li>
                <li><span class="text-sm font-medium text-white/80">Store | Coins: <%= coins %></span></li>
            </ol>
        </nav>
    </div>

    <!-- Store Items -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-4">
      
      <!-- RAM Card -->
      <div class="bg-white/5 rounded-2xl p-6 flex flex-col items-center" x-data="{ quantity: 1, loading: false }">
          <h3 class="text-xl font-semibold mb-2">RAM</h3>
          <p class="text-gray-400 text-sm mb-3">Upgrade your server's memory</p>
          <span class="text-lg font-medium">400 Coins / GB</span>
          <div class="flex items-center space-x-2 my-3">
              <button @click="if(quantity > 1) quantity--" class="bg-gray-700 px-2 py-1 rounded">➖</button>
              <span class="text-lg font-medium w-8 text-center" x-text="quantity"></span>
              <button @click="quantity++" class="bg-gray-700 px-2 py-1 rounded">➕</button>
          </div>
          <button 
            @click="loading = true; window.location.href = `/buyresource/ram?quantity=` + quantity"
            :disabled="loading"
            class="w-full bg-orange-600 font-medium text-white py-2 rounded-full hover:bg-orange-700 transition">
              <span x-show="!loading">Buy RAM</span>
              <span x-show="loading">Processing...</span>
          </button>
      </div>

      <!-- CPU Card -->
      <div class="bg-white/5 rounded-2xl p-6 flex flex-col items-center" x-data="{ quantity: 1, loading: false }">
          <h3 class="text-xl font-semibold mb-2">CPU</h3>
          <p class="text-gray-400 text-sm mb-3">Boost your processing power</p>
          <span class="text-lg font-medium">500 Coins / Core</span>
          <div class="flex items-center space-x-2 my-3">
              <button @click="if(quantity > 1) quantity--" class="bg-gray-700 px-2 py-1 rounded">➖</button>
              <span class="text-lg font-medium w-8 text-center" x-text="quantity"></span>
              <button @click="quantity++" class="bg-gray-700 px-2 py-1 rounded">➕</button>
          </div>
          <button 
            @click="loading = true; window.location.href = `/buyresource/cpu?quantity=` + quantity"
            :disabled="loading"
            class="w-full bg-orange-600 font-medium text-white py-2 rounded-full hover:bg-orange-700 transition">
              <span x-show="!loading">Buy CPU</span>
              <span x-show="loading">Processing...</span>
          </button>
      </div>

      <!-- Disk Card -->
      <div class="bg-white/5 rounded-2xl p-6 flex flex-col items-center" x-data="{ quantity: 1, loading: false }">
          <h3 class="text-xl font-semibold mb-2">Disk</h3>
          <p class="text-gray-400 text-sm mb-3">Expand your storage capacity</p>
          <span class="text-lg font-medium">400 Coins / 10GB</span>
          <div class="flex items-center space-x-2 my-3">
              <button @click="if(quantity > 1) quantity--" class="bg-gray-700 px-2 py-1 rounded">➖</button>
              <span class="text-lg font-medium w-8 text-center" x-text="quantity"></span>
              <button @click="quantity++" class="bg-gray-700 px-2 py-1 rounded">➕</button>
          </div>
          <button 
            @click="loading = true; window.location.href = `/buyresource/disk?quantity=` + quantity"
            :disabled="loading"
            class="w-full bg-orange-600 font-medium text-white py-2 rounded-full hover:bg-orange-700 transition">
              <span x-show="!loading">Buy Disk</span>
              <span x-show="loading">Processing...</span>
          </button>
      </div>

    </div>
  </main>

  <%- include('./components/footer') %>

</body>
</html>
